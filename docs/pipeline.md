# Pipeline

- Phase2: matching artifacts (`pairs.json`, `matches/*.npz`)
- Phase3: tracks + initial points (`ba_input/*.csv`, metadata)
- Phase4: BA solve (`ba_results/*.json`)
- Phase5: rpc refit + plots + run-all orchestration
